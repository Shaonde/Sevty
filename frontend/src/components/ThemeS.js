import { createTheme } from "@material-ui/core";

const ThemeS = createTheme({
    palette: {
        primary: {
            main: '#6B96C2',
          },
          secondary: {
            main: '#415B75',
          },
    },
  });

export default createTheme(ThemeS);